const express = require("express")
const router = express.Router()

router.get("/", () => {})
router.get("/:id", () => {})
router.post("/", () => {})
router.patch("/:id", () => {})
router.put("/:id", () => {})
router.delete("/:id", () => {})

module.exports = router;
