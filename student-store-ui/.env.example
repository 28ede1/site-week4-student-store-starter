# Backend API base URL (no trailing slash).
# For local dev, leave unset to default to http://localhost:5000
# For deployment, set to your deployed backend, e.g.:
# VITE_API_URL=https://your-backend.onrender.com
VITE_API_URL=
